"""ValueScope — open-source, AI-explained investment valuation engine.

All numbers come from this deterministic, versioned calculation engine.
The AI layer only narrates; it never computes (see PRD P2).
"""

ENGINE_VERSION = "1.0.0"

__all__ = ["ENGINE_VERSION"]
